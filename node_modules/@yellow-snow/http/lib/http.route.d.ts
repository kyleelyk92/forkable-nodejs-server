/// <reference types="express" />
import { Route } from "@yellow-snow/core";
import * as express from "express";
import { HttpController } from ".";
import { HttpVerb } from "./http-verb";
export declare class HttpRoute<T extends HttpController> implements Route<T> {
    path: string;
    verb: HttpVerb;
    controller: {
        new (...args: any[]): T;
    };
    method: keyof T;
    middleware: express.RequestHandler | undefined;
    constructor(path: string, verb: HttpVerb, controller: {
        new (...args: any[]): T;
    }, method: keyof T, middleware?: express.RequestHandler);
}

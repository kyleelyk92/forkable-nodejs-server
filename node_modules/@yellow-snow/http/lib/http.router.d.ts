/// <reference types="express" />
import { Router } from "@yellow-snow/core";
import * as express from "express";
export declare class HttpRouter extends Router {
    init(router: express.Router): void;
}
